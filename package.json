{
    "name": "update-egroll",
    "version": "0.1.0",
    "description": "Update egroll proton. Download latest release from github and put it to steam directory.",
    "main": "index.js",
    "dependencies": {},
    "devDependencies": {
        "@types/chai": "^4.2.14",
        "@types/mocha": "^8.2.0",
        "@types/node": "^14.14.16",
        "chai": "^4.2.0",
        "mocha": "^8.2.1",
        "nodemon": "^2.0.6",
        "rimraf": "^3.0.2",
        "ts-node": "^9.1.1",
        "typescript": "^4.1.3"
    },
    "scripts": {
        "start": "npm run build && node ./dist/index.js",
        "dev": "nodemon --exec ts-node ./src/index.ts --watch src --ext .ts",
        "test": "mocha -r ts-node/register src/**/*.test.ts",
        "test:w": "mocha --require ts-node/register --watch --watch-files src, src/**/*.test.ts -R nyan",
        "prebuild": "rimraf ./dist",
        "build": "tsc"
    },
    "keywords": [],
    "author": "undg",
    "license": "Beerware"
}
